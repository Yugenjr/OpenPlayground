name: Welcome Contributors

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - name: Add welcome comment
        uses: actions/github-script@v7
        with:
          script: |
            const isIssue = context.eventName === 'issues';
            const number = isIssue
              ? context.payload.issue.number
              : context.payload.pull_request.number;

            const body = `
👋 **Welcome & Thanks for Contributing!**

We really appreciate you taking the time to contribute 💙

⭐ **If you like this project, don’t forget to star the repository** — it helps the project grow!

🚀 **Looking for ideas, learning resources, or fun projects?**
Check out **Linkshala**:
🔗 https://linkshala.com

You’ll find:
- 🧩 New project ideas  
- 📚 Learning resources  
- 🎮 Games & creative inspiration  

Happy contributing! ✨
`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: number,
              body: body,
            });
